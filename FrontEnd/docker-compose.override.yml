version: '3.4'

services:
  frontend:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

  apibackend:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

